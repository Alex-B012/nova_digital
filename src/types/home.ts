export interface ServiceItem {
   id: number
   title: string
   text: string
   img: string
}

export type NewsItem = { id: number, title: string, img: string };
export type HomeQuoteItem = { id: number, text: string, author: string };